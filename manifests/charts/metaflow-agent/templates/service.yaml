apiVersion: v1
kind: Service
metadata:
  name: {{ include "metaflow-agent.fullname" . }}
  labels:
    {{- include "metaflow-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metaflow-agent.selectorLabels" . | nindent 4 }}
