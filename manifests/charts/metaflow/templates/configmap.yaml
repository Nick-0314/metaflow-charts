kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "metaflow.fullname" . }}-server
  labels:
    {{- include "metaflow.labels" . | nindent 4 }}
data:
  {{- tpl (toYaml .Values.metaflowServerConfig) $ | nindent 2 }}
