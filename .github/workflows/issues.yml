name: Issues Build Latest Helm Repo

on:
  issue_comment:
    types:
      - created

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    if: startswith(github.event.comment.body, '/build-latest-app) && (github.event.issue.number, '1')  
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      
      - name: test
        run: echo "success"

